import React from 'react';
import AccountHome from '../components/account/AccountHome';

const page = () => {
    return (
        <div>
            <AccountHome />
        </div>
    );
};

export default page;